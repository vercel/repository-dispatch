{
  "name": "@vercel/repository-dispatch-status",
  "version": "0.0.1",
  "description": "GitHub action that sets a status corresponding to the workflow result on the sha received from a repository_dispatch event",
  "license": "MIT",
  "author": "Vercel",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup",
    "prepare": "pnpm run build",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@vercel/repository-dispatch": "workspace:*",
    "eslint-config-custom": "workspace:*",
    "jest": "^29.7.0",
    "ts-config": "workspace:*",
    "ts-jest": "^29.4.0",
    "ts-node": "~10.9.2",
    "tsup": "^8.0.2",
    "typescript": "5.7.3"
  }
}
